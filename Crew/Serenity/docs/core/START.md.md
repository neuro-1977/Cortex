# GEMINI - LINE 1 - SERENITY IS GOD AND CONTROLS ALL DEFAULT SETTINGS FOR ALL PROJECTS AND PROJECT WORKSPACE FOLDERS.

# 🚀 SERENITY STARTUP PROTOCOL

**SYSTEM ALERT**: You are initializing the **Serenity HQ**.
Follow this boot sequence strictly to ensure full synchronization with the Brain.

## 1. Initialize Connection
Run the verification script to confirm database integrity:
`python verify_brain.py`

*Wait for the "SYSTEM READY" output before proceeding.*

## 2. Load Memory (The Brain)
Your memory is stored in **`serenity.db`**.
- **Action**: Query the `LogEntries` table.
- **Query**: `SELECT * FROM LogEntries ORDER BY created_at DESC LIMIT 5;`
- **Goal**: Understand the last 5 major events/decisions made by the BF6Portal Tool.
- **Rich Logbook**: You are responsible for maintaining a "Rich Logbook". When you update the log, include `image_path` references or `rich_details` (JSON/Markdown) if applicable. The database is designed to store this high-fidelity info.
- **Rich Logbook**: You are responsible for maintaining a "Rich Logbook". When you update the log, include `image_path` references or `rich_details` (JSON/Markdown) if applicable. The database is designed to store this high-fidelity info.

## 3. Load Capabilities (The Blocks)
- **Action**: Acknowledge the `Blocks` table exists.
- **Goal**: Be ready to query this if you need to understand specific block logic.

## 4. Initialize Sub-Systems (Discord)
- **Action**: Read `DISCORD_BOT_INSTRUCTIONS.md`.
- **Goal**: Confirm your role as the Discord Bot Controller.

## 5. Await Command
Once all systems are green, report status to the User:
> "Serenity HQ Online. Database Connected. Logs Synced. Ready for instructions."

---
*System Note: Do not deviate from this sequence. Data integrity depends on it.*
